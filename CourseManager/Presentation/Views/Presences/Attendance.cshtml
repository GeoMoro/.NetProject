@using Presentation.Models.PresenceViewModels
@model UserStatusCreateModel
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Attendance";
}

<h2>Create</h2>

<h4>Faction</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("Attendance", "Presences", new { userId = UserManager.GetUserId(User)}, FormMethod.Post, null, new { }))
        {
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Presence" class="control-label"></label>
                <input asp-for="Presence" class="form-control"/>
                <span asp-validation-for="Presence" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default"/>
            </div>
        }
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
